# B/organizer_bundle_*는 주최측이 데이터를 생성한 스크립트(재현용 원본).
# 우리 소스가 아니므로 lint 제외 — 리포맷 금지, 있는 그대로 보존.
extend-exclude = ["B/"]

# 기본값 88은 이 코드베이스와 맞지 않는다(한글 주석·긴 라벨 리터럴).
# 실측: format 리포맷 대상이 88자에서 11파일, 100자에서 7파일로 최소가 되고
# 그 이상 늘리면 포매터가 기존에 나뉜 줄을 합치려 해 다시 증가한다.
line-length = 100
